- generalize the skipping logic for for (;;) loops
  (now we have two places iterating using ->op_sibling and using
   custom logic, and I might have missed some)
- system {"foo"} $a, $b should not put the "foo" in a block AST
